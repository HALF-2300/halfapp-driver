app = "halfapp-backend"
primary_region = "sea"

[build]
  dockerfile = "backend/Dockerfile"

[[services]]
  internal_port = 8000
  processes = ["app"]
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443